const User = require('./user');
const Race = require('./races');
const Quote = require('./quotes');
const Participant = require('./participants');

module.exports = { User, Quote, Race, Participant };
